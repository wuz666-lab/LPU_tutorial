# wrgen_req_merge

## 模块要求：

`wrgen_req_merge` (Write Request Generator - Merge) 的主要功能是将多个**地址连续**的小位宽**写请求**，**合并**为一个**大位宽的写请求**，以**提高**总线或下一级模块的**传输效率**。

类似于**“拼车”**：如果是去往同一个方向（**地址连续**）的乘客（数据），我们可以把他们塞进一辆大巴车（**合并输出**）一次运走；如果地址**不连续（case1）**，或者**大巴车满了（case2）**，或者当前是**最后一笔数据了（case3），**就**必须发车**。

## Parameter

- `MERGE_NUM`: 合并**数量**（比如 2，表示**最多积攒 2 个请求**就必须发送）。
- `ADDR_STEP`: 地址步长（判断**连续**的依据）。

## Interface

| 接口名 | 方向 | 描述 |
| --- | --- | --- |
| `clk`, `rst_n` | input | 时钟与复位 |
| **Input Interface** |  |  |
| `in_vld` | input | 输入请求有效 |
| `in_rdy` | output | 模块准备好接收（反压信号） |
| `in_addr` | input | 输入**地址** |
| `in_dat`, `in_msk` | input | 输入**数据与掩码** |
| `in_lst` | input | 包尾标记（Last），表示当前笔数据是包的最后一笔，**遇到它必须立即发车** |
| **Output Interface** |  |  |
| `out_vld` | output | 输出请求有效 |
| `out_rdy` | input | 下游准备好接收 |
| `out_addr` | output | **合并后**的**首地址** |
| `out_dat`, `out_msk` | output | **合并后**的宽数据（宽度是**输入的 `MERGE_NUM` 倍**） |
| `out_lst` | output | **传递 Last** 属性 |

## 核心逻辑真值表：

不要急着写代码。

对于这种带有**“缓存”(ram: line buffer)**、**“合并”(merge)**、**“条件触发”(condition trigger)**功能的模块，直接写代码很容易漏掉 **corner case**（边界情况）。

我们需要**维护一个核心状态**：**当前缓存里攒了几个请求？** 我们用**计数器** cnt 来**表示**。(storage: ram; 显式说明: counter)

请完成下表的填写。梳理清楚在不同场景下，硬件应该做什么。

1. 存储方面：dat_buffer一共开辟[`MERGE_NUM-1`]个存储**unit**，**最后一笔数据pt，直接拼接发送**

| 场景序号 | 当前 Count (`cnt`) | 输入有效 (`in_vld`) | 输入特征 (地址连续? Last?) | 动作 1: 是否发车? (`out_vld`) | 动作 2: 如何更新 Count? | 动作 3: 缓存行为 |
| --- | --- | --- | --- | --- | --- | --- |
| **1** | 0 | 0 | X | 不发车 | 不更新 | 不缓存 |
| 2 | ≥ 0 且 < MAX | 1 | **地址连续** 且 非 Last | 不发车 | +1更新 | 缓存这笔数据 |
| 3 | ≥ 0 且 < MAX/=MAX | 1 | **地址连续** 且 **是 Last** | 1发车 | 归0 | **不缓存**，提取 `data_buffer` 里现有的 `cnt` 个数据，加上当前的 `in_dat` 拼接成一整拼，作为 `out_dat` 发出。 |
| 4 | ≥ 0 且 < MAX/=MAX | 1 | **地址不连续且不是lst** (断开) | 1发车 | 置1（因为0处存入了新数据） | 将原来存储的数据发走，同时再缓存新数据，`cnt`为1 |
| 5 | = MAX (满) | 1 | **地址连续且不是last** | 1发车 | 归0 | 不缓存，最理想情况，直接拼接发出 |
| 6 | > 0 且 < MAX/=MAX | 1 | **地址不连续** 且 **Last** | 1发车，下个cycle仍然发车 | 先置1（因为cnt=0处存入了这个数据），下个cycle将这个数据发车后再归0存入新数据 | 将原有存储数据发出，存入新数据再发出 |
| 7 | 任意 | 1/0 | flush_pending=1 | 1发车，发送缓存的lst包 | 有新输入就置1，否则就置0 | 接收新输入于cnt=0处，并将存在于cnt=0的lst数据发出 |
|  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |

```markdown

```